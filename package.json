{
  "name": "mecanique-invisible",
  "version": "1.0.0",
  "private": true,
  "description": "Recherche sur les interactions humain-IA — mecanique-invisible.com",
  "scripts": {
    "build": "npx @11ty/eleventy",
    "serve": "npx @11ty/eleventy --serve"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0"
  }
}
