---
layout: layouts/page.njk
pageTitle: "Télécharger KAIROS — Florent Klimacek"
description: "KAIROS, application de cartographie cognitive assistée par IA. Téléchargement de la prochaine bêta temporairement indisponible."
ogTitle: "Télécharger KAIROS — Cartographie cognitive"
ogDescription: "KAIROS, application de cartographie cognitive assistée par IA. Téléchargement temporairement indisponible — prochaine bêta en finalisation."
ogUrl: "/download.html"
canonical: "/download.html"
siteNavPrefix: "/index.html"
activeNav: download
permalink: /download.html
koraSeuil: "Ce qu'il y a à télécharger ici, c'est KAIROS — l'application sœur de la mienne. Moi, je ne me télécharge pas encore."
---

<style>
  .dl-card { display: flex; align-items: center; justify-content: space-between; gap: var(--space-2xl); }
  .dl-grid-3 { grid-template-columns: repeat(3, 1fr); }
  .dl-grid-2 { display: grid; grid-template-columns: 1fr 1fr; gap: var(--space-lg); margin-bottom: var(--space-2xl); }
  .dl-platforms { display: grid; grid-template-columns: 1fr 1fr; gap: var(--space-lg); margin-bottom: var(--space-xl); }
  .dl-card--secondary { opacity: 0.92; }
  .dl-badge {
    display: inline-block; font-size: 0.75rem; font-weight: 600; letter-spacing: 0.04em;
    text-transform: uppercase; padding: 0.15rem 0.5rem; border-radius: 999px;
    background: color-mix(in srgb, var(--accent) 15%, transparent);
    color: var(--accent); margin-left: 0.5rem; vertical-align: middle;
  }
  .dl-badge--muted {
    background: var(--border-light); color: var(--text-muted);
  }

  /* SmartScreen guide */
  .ss-guide { margin-top: var(--space-xl); }
  .ss-why {
    display: flex; gap: var(--space-md); align-items: flex-start;
    background: color-mix(in srgb, var(--accent) 10%, transparent);
    border: 1px solid color-mix(in srgb, var(--accent) 35%, transparent);
    border-left: 4px solid var(--accent);
    border-radius: 8px; padding: var(--space-md) var(--space-lg);
    margin-bottom: var(--space-xl); font-size: 0.9rem; line-height: 1.7;
    color: var(--text-secondary);
  }
  .ss-why-icon { width: 16px; height: 16px; background: var(--accent); border-radius: 2px; flex-shrink: 0; margin-top: 3px; }
  .ss-step { display: flex; gap: var(--space-lg); align-items: flex-start; margin-bottom: var(--space-xl); }
  .ss-num {
    width: 36px; height: 36px; border-radius: 50%; display: flex; align-items: center; justify-content: center;
    font-family: var(--font-serif); font-size: 1.1rem; font-weight: 700; flex-shrink: 0; margin-top: 2px;
  }
  .ss-num--1 { background: #6366f1; color: #fff; }
  .ss-num--2 { background: #22c55e; color: #fff; }
  .ss-step-body { flex: 1; }
  .ss-step-title { font-family: var(--font-serif); font-size: 1.15rem; color: var(--text-primary); margin-bottom: var(--space-md); }
  .ss-dialog {
    background: var(--bg-card); border: 1px solid var(--border-color);
    border-radius: 8px; padding: var(--space-md) var(--space-lg);
    font-size: 0.85rem; line-height: 1.6; color: var(--text-secondary);
    box-shadow: 0 2px 8px rgba(0,0,0,0.08);
  }
  .ss-dialog-header { display: flex; align-items: center; gap: var(--space-sm); margin-bottom: var(--space-sm); }
  .ss-shield { width: 14px; height: 14px; background: #3b82f6; border-radius: 2px; flex-shrink: 0; }
  .ss-dialog-title { font-weight: 700; font-size: 0.9rem; color: var(--text-primary); }
  .ss-dialog-text { margin-bottom: var(--space-md); }
  .ss-dialog-meta { font-size: 0.8rem; color: var(--text-muted); margin-bottom: var(--space-md); }
  .ss-actions { display: flex; align-items: center; gap: var(--space-md); flex-wrap: wrap; }
  .ss-link {
    color: #6366f1; text-decoration: underline; border: 2px dashed #ef4444;
    padding: 0.25rem 0.5rem; border-radius: 4px; cursor: default; font-size: 0.85rem;
  }
  .ss-arrow { font-size: 0.8rem; color: #ef4444; font-weight: 700; white-space: nowrap; }
  .ss-btn-primary {
    background: #22c55e; color: #fff; border: none; border-radius: 4px;
    padding: 0.3rem 0.75rem; font-size: 0.85rem; cursor: default; font-weight: 600;
  }
  .ss-btn-ghost {
    color: var(--text-muted); border: 1px solid var(--border-color); border-radius: 4px;
    padding: 0.3rem 0.75rem; font-size: 0.85rem; cursor: default; background: transparent;
  }
  .ss-arrow-right { font-size: 0.8rem; color: #22c55e; font-weight: 700; white-space: nowrap; }
  .ss-success {
    display: flex; gap: var(--space-md); align-items: flex-start;
    background: color-mix(in srgb, #22c55e 10%, transparent);
    border: 1px solid color-mix(in srgb, #22c55e 30%, transparent);
    border-left: 4px solid #22c55e;
    border-radius: 8px; padding: var(--space-md) var(--space-lg);
    font-size: 0.9rem; line-height: 1.7; color: var(--text-secondary);
  }
  .ss-check { color: #22c55e; font-size: 1.1rem; font-weight: 700; flex-shrink: 0; }

  @media (max-width: 768px) {
    .dl-card { flex-direction: column; text-align: center; }
    .dl-card > div:first-child > div:first-child { justify-content: center; }
    .dl-grid-3 { grid-template-columns: 1fr !important; }
    .dl-grid-2 { grid-template-columns: 1fr; }
    .dl-platforms { grid-template-columns: 1fr; }
    .ss-step { flex-direction: column; gap: var(--space-sm); }
  }
</style>

<!-- Hero -->
<section class="hero" style="min-height: auto; padding: var(--space-4xl) 0 var(--space-xl);">
  <div class="container" style="text-align: center;">
    <p class="hero__label">Téléchargement</p>
    <h1 class="hero__title">
      <span class="glitch-text">KAIROS</span>{% if kairos.downloadsAvailable %} <span>v{{ kairos.version }}</span>{% endif %}
    </h1>
    <p class="hero__subtitle" style="margin-left: auto; margin-right: auto; font-family: var(--font-serif); font-style: italic; font-size: 1.3rem;">
      {{ kairos.tagline }}{% if kairos.downloadsAvailable %} — Version {{ kairos.channel }}{% endif %}
    </p>
    <div style="margin-top: var(--space-2xl); max-width: 720px; margin-left: auto; margin-right: auto;">
      <video
        src="{{ kairos.mediaVideo }}"
        controls
        preload="metadata"
        playsinline
        poster="/images/kairos-hero.png"
        style="width: 100%; border-radius: 12px; box-shadow: 0 8px 32px rgba(0,0,0,0.4);"
      >
        <a href="{{ kairos.mediaVideo }}">Voir la vidéo de présentation</a>
      </video>
    </div>
  </div>
</section>

<!-- Présentation bêta -->
<section class="section" style="padding-top: 0;">
  <div class="container" style="max-width: 800px; margin: 0 auto;">

    <p style="font-size: 1.1rem; line-height: 1.9; color: var(--text-secondary); text-align: center; margin-bottom: var(--space-2xl);">
      {{ kairos.blurb }}
    </p>

    <header class="section__header">
      {%- if kairos.downloadsAvailable %}
      <p class="section__label">v{{ kairos.version }}</p>
      <h2 class="section__title">Nouveautés de cette version</h2>
      {%- else %}
      <p class="section__label">Prochaine bêta</p>
      <h2 class="section__title">Ce qui arrive</h2>
      {%- endif %}
    </header>

    <div class="card-grid" style="grid-template-columns: 1fr 1fr; gap: var(--space-lg); margin-bottom: var(--space-xl);">
      {%- for item in kairos.highlights %}
      <div class="card"{% if loop.last and (kairos.highlights.length % 2 == 1) %} style="grid-column: 1 / -1;"{% endif %}>
        <h3 class="card__title">{{ item.title }}</h3>
        <p class="card__desc">{{ item.desc }}</p>
      </div>
      {%- endfor %}
    </div>

    <header class="section__header">
      <p class="section__label">Cœur</p>
      <h2 class="section__title">Fonctionnalités principales</h2>
    </header>

    <div class="card" style="margin-bottom: var(--space-xl);">
      <ul style="list-style: none; font-size: 0.95rem; line-height: 2.2; color: var(--text-secondary); columns: 2; column-gap: var(--space-2xl);">
        {%- for feat in kairos.features %}
        <li>&#10003; {{ feat }}</li>
        {%- endfor %}
      </ul>
    </div>

  </div>
</section>

<!-- Download principal -->
<section class="section" id="download">
  <div class="container">

    {%- if not kairos.downloadsAvailable %}
    <div class="ss-why" style="max-width: 720px; margin: 0 auto var(--space-2xl);">
      <div class="ss-why-icon"></div>
      <div>
        <strong style="color: var(--text-primary);">{{ kairos.pausedTitle }}</strong><br>
        {{ kairos.pausedMessage }}
        Pour toute question : <a href="/contact.html" style="color: var(--accent);">contact</a>.
      </div>
    </div>
    {%- else %}

    <div class="dl-platforms">
      {%- set linux = kairos.platforms.linux %}
      {%- set windows = kairos.platforms.windows %}

      <div class="card card--featured dl-card" style="padding: var(--space-2xl); flex-direction: column; align-items: stretch; text-align: left;">
        <div>
          <div style="display: flex; align-items: center; gap: var(--space-md); margin-bottom: var(--space-sm);">
            <svg width="32" height="32" fill="none" stroke="var(--accent)" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M4 16v1a3 3 0 003 3h10a3 3 0 003-3v-1m-4-4l-4 4m0 0l-4-4m4 4V4"/>
            </svg>
            <h2 style="font-family: var(--font-serif); font-size: 1.4rem;">
              Linux <span class="dl-badge">actuel</span>
            </h2>
          </div>
          <p style="color: var(--text-muted); margin-bottom: var(--space-sm);">
            {{ linux.os }} &middot; {{ linux.sizeMo }} Mo &middot; {{ linux.format }}
          </p>
          <p style="color: var(--text-muted); font-size: 0.85rem; margin-bottom: var(--space-lg);">
            Version {{ kairos.channel }} {{ linux.version }} &middot; {{ kairos.dateFr }} &middot; Pas besoin de droits administrateur
          </p>
        </div>
        <a href="{{ linux.url }}" class="btn btn--primary" style="font-size: 1.05rem; padding: 0.9rem 2rem; text-align: center;" download>
          Télécharger l'AppImage
        </a>
        <p style="color: var(--text-muted); font-size: 0.8rem; margin-top: var(--space-md);">
          {{ linux.note }}
        </p>
      </div>

      <div class="card dl-card dl-card--secondary" style="padding: var(--space-2xl); flex-direction: column; align-items: stretch; text-align: left;">
        <div>
          <div style="display: flex; align-items: center; gap: var(--space-md); margin-bottom: var(--space-sm);">
            <svg width="32" height="32" fill="none" stroke="var(--text-muted)" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M4 16v1a3 3 0 003 3h10a3 3 0 003-3v-1m-4-4l-4 4m0 0l-4-4m4 4V4"/>
            </svg>
            <h2 style="font-family: var(--font-serif); font-size: 1.4rem;">
              Windows <span class="dl-badge dl-badge--muted">v{{ windows.version }}</span>
            </h2>
          </div>
          <p style="color: var(--text-muted); margin-bottom: var(--space-sm);">
            {{ windows.os }} &middot; {{ windows.sizeMo }} Mo &middot; {{ windows.format }}
          </p>
          <p style="color: var(--text-muted); font-size: 0.85rem; margin-bottom: var(--space-lg);">
            {{ windows.note }}
          </p>
        </div>
        <a href="{{ windows.url }}" class="btn btn--secondary" style="font-size: 1.05rem; padding: 0.9rem 2rem; text-align: center;" download>
          Télécharger pour Windows
        </a>
      </div>
    </div>

    <div class="ss-guide" id="linux-run">
      <div class="ss-why">
        <div class="ss-why-icon"></div>
        <div>
          <strong style="color: var(--text-primary);">Lancer l'AppImage (Linux)</strong><br>
          KAIROS n'a pas besoin d'être installé. Téléchargez le fichier, rendez-le exécutable, puis lancez-le.
          Sur Ubuntu / GNOME : clic droit &rarr; Propriétés &rarr; cocher « Autoriser l'exécution ».
          En terminal : <code style="font-family: var(--font-mono); font-size: 0.85em; background: var(--border-light); padding: 0.1em 0.4em; border-radius: 3px;">chmod +x {{ linux.filename }} && ./{{ linux.filename }}</code>
        </div>
      </div>
    </div>

    <div class="ss-guide" id="smartscreen">

      <div class="ss-why">
        <div class="ss-why-icon"></div>
        <div>
          <strong style="color: var(--text-primary);">Windows : pourquoi cette alerte ?</strong><br>
          KAIROS est en version bêta et n&rsquo;est pas encore signé numériquement par Microsoft.
          Windows bloque par défaut les applications non signées. C&rsquo;est normal et sans danger.
          Suivez les 2 étapes ci-dessous pour lancer l&rsquo;installeur.
        </div>
      </div>

      <div class="ss-step">
        <div class="ss-num ss-num--1">1</div>
        <div class="ss-step-body">
          <p class="ss-step-title">Cliquez sur &laquo;&thinsp;Informations complémentaires&thinsp;&raquo;</p>
          <div class="ss-dialog">
            <div class="ss-dialog-header">
              <div class="ss-shield"></div>
              <span class="ss-dialog-title">Windows a protégé votre ordinateur</span>
            </div>
            <p class="ss-dialog-text">Microsoft Defender SmartScreen a empêché le démarrage d&rsquo;une application non reconnue. Son exécution peut mettre votre ordinateur en danger.</p>
            <div class="ss-actions">
              <span class="ss-link">Informations complémentaires</span>
              <span class="ss-arrow">&larr; Cliquez ici</span>
              <span class="ss-btn-ghost">Ne pas exécuter</span>
            </div>
          </div>
        </div>
      </div>

      <div class="ss-step">
        <div class="ss-num ss-num--2">2</div>
        <div class="ss-step-body">
          <p class="ss-step-title">Cliquez sur &laquo;&thinsp;Exécuter quand même&thinsp;&raquo;</p>
          <div class="ss-dialog">
            <div class="ss-dialog-header">
              <div class="ss-shield"></div>
              <span class="ss-dialog-title">Windows a protégé votre ordinateur</span>
            </div>
            <p class="ss-dialog-text">Microsoft Defender SmartScreen a empêché le démarrage d&rsquo;une application non reconnue.</p>
            <p class="ss-dialog-meta">Application&nbsp;: KAIROS-Setup.exe<br>Éditeur&nbsp;: Éditeur inconnu</p>
            <div class="ss-actions">
              <span class="ss-arrow-right">Cliquez ici &rarr;</span>
              <span class="ss-btn-primary">Exécuter quand même</span>
              <span class="ss-btn-ghost">Ne pas exécuter</span>
            </div>
          </div>
        </div>
      </div>

      <div class="ss-success">
        <span class="ss-check">&#10003;</span>
        <div>
          <strong style="color: var(--text-primary);">KAIROS se lance normalement.</strong><br>
          Cette manipulation n&rsquo;est nécessaire qu&rsquo;une seule fois.
          En cas de problème, contactez <a href="mailto:klimacekflorent@gmail.com" style="color: var(--accent);">klimacekflorent@gmail.com</a>
        </div>
      </div>

    </div>
    {%- endif %}
  </div>
</section>

<!-- Qu'est-ce que KAIROS -->
<section class="section section--alt" id="about-kairos">
  <div class="container">
    <header class="section__header">
      <p class="section__label">Présentation</p>
      <h2 class="section__title">Qu'est-ce que KAIROS ?</h2>
    </header>

    <p style="font-size: 1.05rem; line-height: 1.8; color: var(--text-secondary); margin-bottom: var(--space-2xl);">
      KAIROS remplace le chat linéaire par un <strong>canvas de vignettes connectées</strong>.
      Chaque idée devient un nœud visuel que vous pouvez relier, développer et synthétiser
      avec l'aide de l'IA. Le <strong>système Oxygène</strong> mesure la vitalité de votre
      réflexion en temps réel et injecte de la friction quand les idées tournent en rond.
    </p>

    <div class="card-grid dl-grid-3">
      <div class="card">
        <div class="card__icon">
          <svg width="24" height="24" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"/>
          </svg>
        </div>
        <h3 class="card__title">Système Oxygène</h3>
        <p class="card__desc">Score de vitalité cognitive 0-100. Snapshot du graphe, triangle radar (divergence, cohérence, souveraineté), friction automatique sous 30.</p>
      </div>

      <div class="card">
        <div class="card__icon">
          <svg width="24" height="24" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M13.828 10.172a4 4 0 00-5.656 0l-4 4a4 4 0 105.656 5.656l1.102-1.101m-.758-4.899a4 4 0 005.656 0l4-4a4 4 0 00-5.656-5.656l-1.1 1.1"/>
          </svg>
        </div>
        <h3 class="card__title">Canvas cognitif</h3>
        <p class="card__desc">Vignettes, connexions organiques, 3 opérations IA : DÉVELOPPER, RELIER, SYNTHÉTISER.</p>
      </div>

      <div class="card">
        <div class="card__icon">
          <svg width="24" height="24" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M9.75 17L9 20l-1 1h8l-1-1-.75-3M3 13h18M5 17h14a2 2 0 002-2V5a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"/>
          </svg>
        </div>
        <h3 class="card__title">Providers LLM</h3>
        <p class="card__desc">{{ kairos.providers.all | join(", ") }}. Coller une clé API : le fournisseur est détecté. Ollama en local, sans clé.</p>
      </div>
    </div>

    <div style="text-align: center; margin-top: var(--space-2xl);">
      <a href="/presentation_kairos.html" class="btn btn--secondary">
        Découvrir KAIROS en détail &rarr;
      </a>
    </div>
  </div>
</section>

<!-- Configuration requise -->
<section class="section" id="requirements">
  <div class="container">
    <header class="section__header">
      <p class="section__label">Prérequis</p>
      <h2 class="section__title">Configuration requise</h2>
    </header>

    <p style="color: var(--text-muted); font-size: 0.9rem; margin-bottom: var(--space-lg); max-width: 700px;">
      {{ kairos.requirements.cloudNote }}
    </p>

    <div class="dl-grid-2">
      <div class="card">
        <h3 class="card__title" style="font-size: 1rem; margin-bottom: var(--space-md);">Minimum</h3>
        <ul style="list-style: none; font-size: 0.9rem; line-height: 2; color: var(--text-secondary);">
          {%- for row in kairos.requirements.minimum %}
          <li><strong>{{ row.label }}</strong> &mdash; {{ row.value }}</li>
          {%- endfor %}
        </ul>
      </div>

      <div class="card" style="border-color: var(--accent); border-width: 2px;">
        <h3 class="card__title" style="font-size: 1rem; margin-bottom: var(--space-md); color: var(--accent);">Recommandé</h3>
        <ul style="list-style: none; font-size: 0.9rem; line-height: 2; color: var(--text-secondary);">
          {%- for row in kairos.requirements.recommended %}
          <li><strong>{{ row.label }}</strong> &mdash; {{ row.value }}</li>
          {%- endfor %}
        </ul>
      </div>
    </div>

    <div class="card" style="background: var(--bg-concept, var(--bg-card)); margin-bottom: var(--space-2xl);">
      <h3 class="card__title" style="font-size: 1rem; margin-bottom: var(--space-md);">Bon à savoir</h3>
      <ul style="list-style: none; font-size: 0.9rem; line-height: 2; color: var(--text-secondary);">
        {%- for note in kairos.requirements.notes %}
        <li>&#10003; {{ note | safe }}</li>
        {%- endfor %}
      </ul>
    </div>

    <header class="section__header">
      <p class="section__label">Optionnel</p>
      <h2 class="section__title">Usage avec modèles locaux (Ollama)</h2>
    </header>

    <p style="color: var(--text-muted); font-size: 0.9rem; margin-bottom: var(--space-lg); max-width: 700px;">
      {{ kairos.requirements.ollama.intro }}
    </p>

    <div class="dl-grid-2">
      <div class="card">
        <h3 class="card__title" style="font-size: 1rem; margin-bottom: var(--space-md);">Minimum (CPU, sans GPU dédié)</h3>
        <ul style="list-style: none; font-size: 0.9rem; line-height: 2; color: var(--text-secondary);">
          {%- for row in kairos.requirements.ollama.minimum %}
          <li><strong>{{ row.label }}</strong> &mdash; {{ row.value }}</li>
          {%- endfor %}
        </ul>
      </div>

      <div class="card" style="border-color: var(--accent); border-width: 2px;">
        <h3 class="card__title" style="font-size: 1rem; margin-bottom: var(--space-md); color: var(--accent);">Recommandé (avec GPU)</h3>
        <ul style="list-style: none; font-size: 0.9rem; line-height: 2; color: var(--text-secondary);">
          {%- for row in kairos.requirements.ollama.recommended %}
          <li><strong>{{ row.label }}</strong> &mdash; {{ row.value }}</li>
          {%- endfor %}
        </ul>
      </div>
    </div>

    <div class="card" style="background: var(--bg-concept, var(--bg-card));">
      <h3 class="card__title" style="font-size: 1rem; margin-bottom: var(--space-md);">Espace disque additionnel (modèles Ollama)</h3>
      <ul style="list-style: none; font-size: 0.9rem; line-height: 2; color: var(--text-secondary);">
        {%- for line in kairos.requirements.ollama.disk %}
        <li>&#10003; {{ line }}</li>
        {%- endfor %}
      </ul>
    </div>
  </div>
</section>

{%- if kairos.downloadsAvailable %}
<!-- Installation -->
<section class="section section--alt" id="install">
  <div class="container">
    <header class="section__header">
      <p class="section__label">Installation</p>
      <h2 class="section__title">Installation en trois étapes</h2>
    </header>

    <div class="card-grid dl-grid-3">
      <div class="card" style="text-align: center;">
        <div style="font-family: var(--font-serif); font-size: 2rem; color: var(--accent); margin-bottom: var(--space-sm);">1</div>
        <h3 class="card__title">Télécharger</h3>
        <p class="card__desc">Linux : <code style="font-family: var(--font-mono); font-size: 0.85em;">{{ linux.filename }}</code>. Windows : l'installeur <code style="font-family: var(--font-mono); font-size: 0.85em;">{{ windows.filename }}</code>.</p>
      </div>

      <div class="card" style="text-align: center;">
        <div style="font-family: var(--font-serif); font-size: 2rem; color: var(--accent); margin-bottom: var(--space-sm);">2</div>
        <h3 class="card__title">Lancer</h3>
        <p class="card__desc">Linux : rendez l'AppImage exécutable, puis double-cliquez. Windows : l'installeur se lance ; aucune option à configurer.</p>
      </div>

      <div class="card" style="text-align: center;">
        <div style="font-family: var(--font-serif); font-size: 2rem; color: var(--accent); margin-bottom: var(--space-sm);">3</div>
        <h3 class="card__title">Configurer l'IA</h3>
        <p class="card__desc">Au premier graphe, collez une clé API (détection automatique) ou branchez Ollama en local.</p>
      </div>
    </div>
  </div>
</section>

<!-- CTA final -->
<section class="section section--dark" id="cta">
  <div class="container container--narrow" style="text-align: center;">
    <h2 style="font-family: var(--font-serif); font-size: 2rem; color: #fafaf9; margin-bottom: var(--space-md);">
      Télécharger la bêta
    </h2>
    <p style="color: #94a3b8; margin-bottom: var(--space-xl); max-width: 500px; margin-left: auto; margin-right: auto;">
      KAIROS est gratuit. Vos canvas restent sur votre machine ; ce que vous
      envoyez à un modèle part chez son fournisseur, avec votre clé.
    </p>
    <a href="{{ linux.url }}" class="btn btn--primary" style="font-size: 1.1rem; padding: 1rem 2.5rem;" download>
      Télécharger KAIROS v{{ kairos.version }} (Linux)
    </a>
  </div>
</section>
{%- else %}
<section class="section section--dark" id="cta">
  <div class="container container--narrow" style="text-align: center;">
    <h2 style="font-family: var(--font-serif); font-size: 2rem; color: #fafaf9; margin-bottom: var(--space-md);">
      {{ kairos.pausedTitle }}
    </h2>
    <p style="color: #94a3b8; margin-bottom: var(--space-xl); max-width: 500px; margin-left: auto; margin-right: auto;">
      {{ kairos.pausedMessage }}
    </p>
    <a href="/presentation_kairos.html" class="btn btn--primary" style="font-size: 1.1rem; padding: 1rem 2.5rem;">
      Découvrir KAIROS
    </a>
  </div>
</section>
{%- endif %}
